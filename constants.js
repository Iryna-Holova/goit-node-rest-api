const MIN_PASSWORD = 6;

const EMAIL_REGEXP = /^\w+([\.-]?\w+)*@\w+([\.-]?\w+)*(\.\w{2,3})+$/;

const SUBSCRIPTIONS = ["starter", "pro", "business"];

export default { MIN_PASSWORD, EMAIL_REGEXP, SUBSCRIPTIONS };
